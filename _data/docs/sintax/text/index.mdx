---
title: "Text"
description: "Trim, case-shift, slugify, split, and reshape strings."
---

## Text

| Modifier | Description |
|----------|-------------|
| [`concat`](./concat) | Concat appends one or more strings to the value. |
| [`join`](./join) | Join combines an array of strings into a single string with a separator. |
| [`lines`](./lines) | Lines splits a string or byte slice into an array of lines. |
| [`lower`](./lower) | ToLower converts a string to lowercase. |
| [`replace`](./replace) | Replace replaces all occurrences of a substring within the string value. |
| [`replace_pattern`](./replace_pattern) | ReplacePattern replaces all regex matches within the string value. |
| [`reverse`](./reverse) | Reverse reverses the characters in a string. |
| [`sexy`](./sexy) | Sexy returns a bear ASCII art. |
| [`shorten`](./shorten) | Shorten truncates a string to the given maximum character length. |
| [`slug`](./slug) | Slug converts a string to a URL-friendly slug. |
| [`split`](./split) | Split splits a string into an array using a separator. |
| [`title`](./title) | Title converts a hyphen-separated slug into a title-cased string. |
| [`title_model`](./title_model) | ModelTitle formats an AI model identifier into a human-readable title. |
| [`trim`](./trim) | Trim removes leading and trailing whitespace, or the given character set. |
| [`trim-prefix`](./trim-prefix) | TrimPrefix removes a leading prefix string or leading whitespace from the value. |
| [`trim-suffix`](./trim-suffix) | TrimSuffix removes a trailing suffix string or trailing whitespace from the value. |
| [`upper`](./upper) | ToUpper converts a string to uppercase. |

